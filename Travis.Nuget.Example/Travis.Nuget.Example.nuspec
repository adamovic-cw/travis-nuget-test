<?xml version="1.0"?>
<package xmlns="http://schemas.microsoft.com/packaging/2010/07/nuspec.xsd">
    <metadata>
        <!-- The identifier that must be unique within the hosting gallery -->
        <id>Travis.Nuget.Example</id>
        <title>Travis NuGet Example</title>

        <!-- The package version number that is used when resolving dependencies -->
        <version>$version$</version>

        <!-- Authors contain text that appears directly on the gallery -->
        <authors>slobodan</authors>

        <!-- Owners are typically nuget.org identities that allow gallery
             users to earily find other packages by the same owners.  -->
        <owners>slobodan</owners>
        <requireLicenseAcceptance>false</requireLicenseAcceptance>
        <developmentDependency>false</developmentDependency>

        <!-- The description can be used in package manager UI. Note that the
             nuget.org gallery uses information you add in the portal. -->
        <description>The example project for testing deployment to nuget using travis-ci.</description>
        <tags>travis ci nuget example</tags>

        <!-- Dependencies are automatically installed when the package is installed -->
        <dependencies>
            <group targetFramework="net45" >
              <dependency id="RestSharp" version="105.2.3" />
            </group>
        </dependencies>
    </metadata>
    <files>

        <!-- A readme.txt will be displayed when the package is installed -->
        <file src="../bin/Travis.Nuget.Example.dll" target="lib/net45" />
        <file src="../bin/Travis.Nuget.Example.xml" target="lib/net45" />
    </files>
</package>
